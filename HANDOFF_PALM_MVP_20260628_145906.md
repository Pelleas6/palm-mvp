# HANDOFF PALM-MVP — Reprise projet lecture de main

1. État résumé
- Date: Sun Jun 28 14:59:06 UTC 2026
- Projet: /opt/data/projects/palm-mvp
- Repo: Pelleas6/palm-mvp
- Dernier commit local: 2f4516e
- Push effectué: non
- Déploiement effectué: non
- État build: OK
- État Git: Workspace propre (commits faits, fichiers .bak ignorés)

2. Ce qui a été fait
- Audit initial et correction des permissions (root -> hermes)
- Sécurisation des routes API (/api/upload, /api/create-checkout, /api/stripe-webhook)
- Mise en place architecture session OTP (Redis + palm_session cookie)
- Implémentation request_id côté serveur (fichiers : /api/upload, /api/create-checkout, /api/stripe-webhook, /api/verify-otp)
- Ajout protection Origin/Referer sur les routes POST
- Configuration .gitignore et package-lock.json

3. Architecture retenue
- Option A stricte : Auth via Redis (cookie httpOnly) + Backend protected routes + Signed URLs. Aucun accès client direct aux bucket photos (privé). Webhook protégé par constructEvent.

4. Fichiers importants modifiés
- .gitignore
- package-lock.json
- app/api/verify-otp/route.js
- app/api/upload/route.js
- app/api/create-checkout/route.js
- app/api/stripe-webhook/route.js

5. Supabase
- Bucket : palm-uploads (Privé confirmé manuellement, aucune policy publique)

6. Vercel (Variables à configurer manuellement)
- Liste : SITE_URL, STRIPE_PRICE_ID, SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RESEND_API_KEY.
- *Attention : SITE_URL et STRIPE_PRICE_ID doivent être ajoutés.*

7. Ce qu’il reste à faire
- 1. Ajouter variables Vercel (SITE_URL, STRIPE_PRICE_ID).
- 2. Faire un dernier build local.
- 3. Autoriser git push.
- 4. Valider le déploiement.
- 5. Tester le flux complet (OTP -> Upload -> Checkout -> Webhook).

8. Prompt de reprise
"Reprends le projet palm-mvp à partir du fichier /opt/data/projects/palm-mvp/HANDOFF_PALM_MVP.md. Commence par le lire, puis fais un contrôle local : git status, git log -1, npm run build, variables attendues. Ne push pas et ne déploie pas sans autorisation."
